# Required — set your bot token from @BotFather
BOT_TOKEN=your-telegram-bot-token

# Optional — defaults to local Docker Compose setup
# DATABASE_URL=postgres://secretme:secretme@localhost:5432/secretme?sslmode=disable

# Optional — auto-generated with random 32-byte salt if not set
# ENC_SALT=your-encryption-salt
